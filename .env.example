# Configure production values as secret environment variables in Sites.
# Never commit actual values. Local cloud runtime requires macOS 13.5+ or Linux.
GATHER_ENCRYPTION_KEY=
GATHER_OWNER_EMAIL=
GATHER_ORIGIN=
